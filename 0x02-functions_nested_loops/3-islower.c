#include "main.h"
#include "_putchar.c"

/**
 * _islower - This is a description
 * @c: This is an integer the user input
 *
 * Return: 1 if islower else return 0
 */
int _islower(int c)
{
if (c >= 97 && c <= 123)
return (0);
else
return (1);
}
