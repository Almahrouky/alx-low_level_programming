#include "main.h"

/**
 * _strncat - is a function
 * @dest: is a pinter char
 * @src: is a pointer char
 * @n: is an integer
 * Return: poionter char
 */
char *_strncat(char *dest, char *src, int n)
{
return (strncat(dest, src, n));
}
