#include "main.h"

/**
 * _abs - This is a description
 * @a: This is an integer the user input
 *
 * Return: 1 someting
 */
int _abs(int a)
{
if (a > 0)
return (a);
else if (a < 0)
return (-a);
else
return (0);
}
