#include "main.h"

/**
 * main - is the main function
 * @argc: is an integer
 * @argv: is is a pointer char
 * Return: an integer
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d \n", argc);
return (0);
}
